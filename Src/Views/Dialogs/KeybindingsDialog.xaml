<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MPDCtrl.Views.Dialogs.KeybindingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MPDCtrl.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:vm="using:MPDCtrl.ViewModels"
    xmlns:mod="using:MPDCtrl.Models"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer Margin="10,10,0,10" Width="480" Height="250" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="0,0,12,0">
            <StackPanel Spacing="14" Margin="0,0,0,24">
                
                <TextBlock Text="Application Global" FontWeight="Bold" FontSize="16"/>
                <StackPanel Spacing="14" Margin="12,0,0,0">
                    <TextBlock>
                        <Run Text="Play/Pause: " FontWeight="SemiBold"/>
                        <Run Text="MediaPlay, MediaPlayPause, MediaStop, Ctrl+P, Space(when not focused on list)"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Play Next: " FontWeight="SemiBold"/>
                        <Run Text="MediaNextTrack, Ctrl+N, Ctrl+Right"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Play Previous: " FontWeight="SemiBold"/>
                        <Run Text="MediaPreviousTrack, Ctrl+B, Ctrl+Left"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Volume Mute: " FontWeight="SemiBold"/>
                        <Run Text="MediaVolumeMute"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Volume Down: " FontWeight="SemiBold"/>
                        <Run Text="VolumeDown, Ctrl+Subtract"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Volume Up: " FontWeight="SemiBold"/>
                        <Run Text="VolumeUp, Ctrl+Add"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Search library: " FontWeight="SemiBold"/>
                        <Run Text="Ctrl+S"/>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="Queue page" FontWeight="Bold" FontSize="16"/>
                <StackPanel Spacing="14" Margin="12,0,0,0">
                    <TextBlock>
                        <Run Text="(On the list) Play selected: " FontWeight="SemiBold"/>
                        <Run Text="Enter"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="(On the list) Remove selected: " FontWeight="SemiBold"/>
                        <Run Text="Ctrl+Delete"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Find song in the Queue: " FontWeight="SemiBold"/>
                        <Run Text="Ctrl+F"/>
                    </TextBlock>
                </StackPanel>

                <TextBlock Text="Albums page" FontWeight="Bold" FontSize="16"/>
                <StackPanel Spacing="14" Margin="12,0,0,0">
                    <TextBlock>
                        <Run Text="Find album in the Albums: " FontWeight="SemiBold"/>
                        <Run Text="Ctrl+F"/>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="Artists page" FontWeight="Bold" FontSize="16"/>
                <StackPanel Spacing="14" Margin="12,0,0,0">
                    <TextBlock>
                        <Run Text="Find artist in the Artists: " FontWeight="SemiBold"/>
                        <Run Text="Ctrl+F"/>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="Files page" FontWeight="Bold" FontSize="16"/>
                <StackPanel Spacing="14" Margin="12,0,0,0">
                    <TextBlock>
                        <Run Text="Filter files in the Files: " FontWeight="SemiBold"/>
                        <Run Text="Ctrl+F"/>
                    </TextBlock>
                </StackPanel>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
